export const useHooks = () => {
  const regexs = [
    /^.{3,}$/,
    /^[a-zA-Z0-9._%+-]+@gmail\.com$/,
    /^(?=.*[!@#$%^&*()_+{}\]:;<>,.?~/-]).{6,}$/,
    /^(?=.*[!@#$%^&*()_+{}\]:;<>,.?~/-]).{6,}$/,
  ];

  return regexs;
};
